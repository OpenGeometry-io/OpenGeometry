# opengeometry-babylon

Scaffold, not yet implemented. A future Babylon.js adapter for the OpenGeometry kernel.

For the current public SDK, see `main/opengeometry-three/` and the published
[`opengeometry`](https://www.npmjs.com/package/opengeometry) package.

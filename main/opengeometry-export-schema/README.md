# opengeometry-export-schema

Scaffold, not yet implemented. Reserved for future export-schema definitions.

For current export functionality, see `main/opengeometry/src/export/` (STEP, STL, IFC,
projection, native PDF).
